language: python
python:
  - 3.6
services:
  - mongodb
install:
  - pip install -r requirements/develop.pip codecov
script:
  - inv qa cover --report
after_success:
  - codecov --file=reports/coverage.xml
